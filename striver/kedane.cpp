#include <bits/stdc++.h>
using namespace std;

 int main()
 {
  
   int ar[]={7,1,5,3,6,4};
   vector<int> v(ar,ar+6);
   vector<int> vi;
   copy(v.begin(),v.end(),vi.begin());
   sort(vi.begin(),vi.end());
   int i=0;
   while(i<=vi.size()/2)
   {
      if(vi.find(vi.begin(), vi.end(), 3))
   }
 
    
   


 }